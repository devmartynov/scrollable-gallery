// Конфигурации для приложения. (prod, dev, preview, ...)
// Может быть вынесена в env переменные
import ProdConfig from './ProdConfig';
import DevConfig from './DevConfig';

export {
    ProdConfig,
    DevConfig,
}
